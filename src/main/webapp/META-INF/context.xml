<?xml version='1.0' encoding='utf-8'?>
<Context>
    <WatchedResource>WEB-INF/web.xml</WatchedResource>

    <Resource name="jdbc/tapadm"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              minEvictableIdleTimeMillis="30000"
              maxActive="${tapadm.maxActive}" maxIdle="${tapadm.maxActive}" maxWait="20000" initialSize="1"
              username="${tapschemauser.username}" password="${tapschemauser.password}"
              driverClassName="${tapschemauser.driverClassName}"
              url="${tapschemauser.url}"
              removeAbandoned="false"
              logAbandoned="true"
              testOnBorrow="true"
              testWhileIdle="true"
              validationQuery="SELECT 1"
              defaultAutoCommit="true"
    />

    <Resource name="jdbc/tapuser"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              minEvictableIdleTimeMillis="30000"
              maxActive="${tapuser.maxActive}" maxIdle="${tapuser.maxActive}" maxWait="20000" initialSize="1"
              username="${tapschemauser.username}" password="${tapschemauser.password}"
              driverClassName="${tapschemauser.driverClassName}"
              url="${tapschemauser.url}"
              removeAbandoned="false"
              logAbandoned="true"
              testOnBorrow="true"
              testWhileIdle="true"
              validationQuery="SELECT 1"
              defaultAutoCommit="true"
    />

    <Resource name="jdbc/qservuser"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              minEvictableIdleTimeMillis="30000"
              maxActive="${qservuser.maxActive}" maxIdle="1" maxWait="20000" initialSize="0"
              username="${qservuser.username}" password="${qservuser.password}"
              driverClassName="${qservuser.driverClassName}"
              url="${qservuser.url}"
              removeAbandoned="false"
              logAbandoned="true"
              testOnBorrow="true"
              testWhileIdle="true"
              validationQuery="SELECT 1"
              defaultAutoCommit="true"
    />

    <Resource name="jdbc/uws"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              minEvictableIdleTimeMillis="30000"
              maxActive="${uws.maxActive}" maxIdle="${uws.maxActive}" maxWait="20000" initialSize="3" minIdle="2"
              username="${uws.username}" password="${uws.password}"
              driverClassName="${uws.driverClassName}"
              url="${uws.url}"
              removeAbandoned="false"
              removeAbandonedTimeout="60"
              testOnReturn="true"
              testWhileIdle="true"
              logAbandoned="true"
              testOnBorrow="true"
              validationInterval="30000"
              validationQuery="SELECT 1"
    />
</Context>
